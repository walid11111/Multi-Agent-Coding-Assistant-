


# self  details 

 python -m venv venv  
 .\venv\Scripts\Activate 

Simplest option — just run:
pip install -e .
That will read pyproject.toml and install all dependencies (langgraph, langchain-groq, pydantic, etc.). Then:
streamlit run app.py




today work:
run straemlit but the interface and styling i did not like create a wireffrime usin louy and create best style and also show the code and text generatio like cloudy  and then look next best faeture


# git commands
Open VS Code
        ↓
Activate venv
        ↓
git pull
        ↓
Code
        ↓
Test
        ↓
git status
        ↓
git add .
        ↓
git commit -m "Today's changes"
        ↓
git push