# Tier 2 — Strong differentiators
4. Streamlit/React frontend with live file tree + code preview
You already planned this. Add:

A live-updating file tree as files get created
Syntax-highlighted code viewer
A "Run" button that spins up the generated project (e.g., via a sandboxed iframe for web apps)

5. Support multiple tech stacks intelligently
Right now it sounds tied to HTML/CSS/JS. Let the Planner choose between stacks (React, Python/Flask API, Node/Express) based on the prompt, and have stack-specific prompt templates for the Architect/Coder.